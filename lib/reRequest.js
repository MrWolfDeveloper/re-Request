/* -------------------------------------------------------------------------- */
/*                   Added by Mr. Wolf ( November 11, 2021 )                  */
/* -------------------------------------------------------------------------- */

var reRequest = reRequest || {};

reRequest = {
	reRequest: (options) => {
		const { dbType, proxy, errorCodes } = options;
	}
};

exports.reRequest = reRequest;
